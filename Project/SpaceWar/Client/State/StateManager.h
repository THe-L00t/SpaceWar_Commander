#pragma once

// ============================================================
//  Client/State/StateManager.h — 아키텍처 명세서 11절
//
//  클라이언트 상태를 임시로 들고, 서버가 보낸 권위 상태와 비교·검증한다.
//  어긋나면 서버 상태로 갱신한다.
//
//  ★ 권위를 갖지 않는다. 게임 상태를 스스로 결정하지 않는다 (명세 18절 원칙 4).
//  ★ 실행 위치 — 메인 스레드 (「멀티스레딩 분류 명세서」 5장·13장)
//    하는 일은 ② 수신 버퍼 교환과 Player 보정(되감기·재적용)이다.
//    비교 대상이 로컬 Player 하나라 병렬 비교는 하지 않는다 (11장 «하지 않는 것»).
//  ★ 클라이언트에만 있다. 서버는 자기 상태가 곧 권위라 비교할 대상이 없다.
//  ★ 지금은 선언만 있다. 예측·보정 규칙(입력 번호 기준 재적용 등)은 미정이다.
// ============================================================

namespace swc {

	class StateManager
	{
	public:
		StateManager();
		~StateManager();
	};

} // namespace swc
